control-c (^c) will interrupt a program at the earliest possible
	moment ,i.e. after the debugged process has executed a
	line, entered a body or exited a body.
	If a R(un command is given the debugged process will
	continue where it was interrupted.

control-l (^l) will clear the output screen.

control-d (^d) exits the debugger.

