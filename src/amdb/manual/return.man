

RETURN                     Adb manual                      RETURN


_N_A_M_E
     <return>

_M_O_D_E_S
     main , run , print , trace , break , delete

_D_E_S_C_R_I_P_T_I_O_N

     Whenever the  prompt  enter  command  is  displayed  on  the
     command  line  the <return> key may be used to return to the
     previous mode which then becomes the current mode.
     Some commands will also display  a  prompt  on  the  command
     line,  in  this  case  typing  <return>  will  terminate the
     command.
     After an error message is given on the command line the only
     way to respond is by typing a single return.

_E_R_R_O_R_S
     by typing <return> after the prompt 'enter kind' in the 'cat'
     command the kind is set to PB (package body).

_S_E_E _A_L_S_O
     cat , quit






































