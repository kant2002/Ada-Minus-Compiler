/*
 *	@(#)ldfile	4.1	ACE (c)
 *	COFF loader file, representing former 407 AOUT format.
 */
SECTIONS {
	GROUP : {
		.text ( : READ,WRITE,EXEC ) : {},
		.data ( : READ,WRITE ) : {},
		.bss  ( : READ,WRITE,UPEXT ) : {}
	}
}
